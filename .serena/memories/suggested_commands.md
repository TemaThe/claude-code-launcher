# Suggested Commands

- Build CLI: `go build ./cmd/ccx`
- Run tests: `go test ./...`
- Format Go files: `gofmt -w <files>`
- Smoke dry-run once built: `./ccx --dry-run min`
- Repo inspection commands used here are standard Linux shell forms (`find`, `sed`, `ls`, `git status`).